# In development, Vite proxies /api/* → http://127.0.0.1:5000/api
# so the default below works out of the box.
# In production (Render / Railway / etc.) set this to your deployed backend URL.
VITE_API_URL=/api
